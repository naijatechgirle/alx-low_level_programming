#include <stdio.h>

/**
 * main - entry point
 *
 *  program that prints the lowercase
 *  alphabet in reverse
 *
 *  Return: 0
 */

int main(void)
{
	char letter = 'z';

	for (letter = 0; letter >= 'a'; letter--)
	{
		putchar(letters);
	}
	putchar('\n');

	return (0);
}
