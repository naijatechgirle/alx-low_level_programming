#include "main.h"
/**
 * _abs - fxn that computes the absolute value of an integar
 * @n: the int to chack
 * Return: the absolute value of int
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
