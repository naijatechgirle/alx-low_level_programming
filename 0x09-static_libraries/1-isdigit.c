#include "main.h"
/**
 * _isdigit - fxn that checks for a digit (0 through 9)
 * @c: int to be checked
 * Return: 1 i c is a digit, else 0
 */

int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}
