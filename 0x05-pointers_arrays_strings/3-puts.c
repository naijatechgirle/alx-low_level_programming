#include "main.h"
#include <stdio.h>
/**
 * _puts - prints string
 * @str: string to be printed
 * Return: void
 */
void _puts(char *str)
{
	int count = 0;

	while (count >= 0)
	{
		if (str[count] == '\0';
			_putchar('\n');

		_putchar(str[count]);
		count++;
	}
}
